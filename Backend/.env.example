# Email Configuration (for testing - use secure production values)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Dentrix API Configuration (configure with actual Dentrix API details)
DENTRIX_API_URL=https://api.dentrix.com/v1
DENTRIX_API_KEY=your-dentrix-api-key

# Other Configuration
OLLAMA_HOST=http://ollama:11434
WHISPER_MODEL=tiny
HF_TOKEN=

# Security Note: 
# - For production, use environment-specific secure credential management
# - Never commit real API keys or passwords to version control
# - Consider using Azure Key Vault, AWS Secrets Manager, or similar services
# - Use proper HIPAA-compliant email services for production deployment